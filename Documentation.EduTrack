<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EduTrack - Complete Documentation</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        :root {
            --primary: #3498db;
            --secondary: #2c3e50;
            --accent: #e74c3c;
            --light: #ecf0f1;
            --dark: #2c3e50;
            --success: #2ecc71;
            --warning: #f39c12;
        }
        
        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            color: #333;
            min-height: 100vh;
        }
        
        .container {
            display: flex;
            max-width: 1400px;
            margin: 0 auto;
            box-shadow: 0 0 30px rgba(0, 0, 0, 0.1);
            min-height: 100vh;
        }
        
        /* Sidebar Navigation */
        .sidebar {
            width: 260px;
            background: var(--secondary);
            color: white;
            padding: 20px 0;
            overflow-y: auto;
            height: 100vh;
            position: sticky;
            top: 0;
        }
        
        .logo {
            padding: 0 20px 20px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            margin-bottom: 20px;
        }
        
        .logo h1 {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 1.5rem;
        }
        
        .logo i {
            color: var(--primary);
        }
        
        .nav-links {
            list-style: none;
        }
        
        .nav-links li {
            margin-bottom: 5px;
        }
        
        .nav-links a {
            color: #ecf0f1;
            text-decoration: none;
            display: block;
            padding: 12px 20px;
            border-left: 4px solid transparent;
            transition: all 0.3s;
        }
        
        .nav-links a:hover, .nav-links a.active {
            background: rgba(255, 255, 255, 0.1);
            border-left-color: var(--primary);
        }
        
        .nav-links i {
            margin-right: 10px;
            width: 20px;
            text-align: center;
        }
        
        /* Main Content */
        .main-content {
            flex: 1;
            background: white;
            padding: 0;
            overflow-y: auto;
            height: 100vh;
        }
        
        .header {
            background: white;
            padding: 20px 30px;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 100;
        }
        
        .search-box {
            display: flex;
            align-items: center;
            background: #f5f7fa;
            border-radius: 50px;
            padding: 8px 20px;
            width: 300px;
        }
        
        .search-box input {
            border: none;
            background: transparent;
            padding: 5px;
            width: 100%;
            outline: none;
        }
        
        .user-actions {
            display: flex;
            align-items: center;
            gap: 20px;
        }
        
        .btn {
            padding: 10px 20px;
            border-radius: 50px;
            border: none;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .btn-primary {
            background: var(--primary);
            color: white;
        }
        
        .btn-outline {
            background: transparent;
            border: 2px solid var(--primary);
            color: var(--primary);
        }
        
        /* Documentation Content */
        .docs-container {
            padding: 30px;
            max-width: 1000px;
            margin: 0 auto;
        }
        
        .docs-content {
            background: white;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            line-height: 1.7;
        }
        
        .docs-content h1 {
            color: var(--secondary);
            border-bottom: 2px solid var(--primary);
            padding-bottom: 15px;
            margin-bottom: 25px;
        }
        
        .docs-content h2 {
            color: var(--secondary);
            margin: 30px 0 15px;
            padding-bottom: 10px;
            border-bottom: 1px solid #eee;
        }
        
        .docs-content h3 {
            color: var(--primary);
            margin: 25px 0 15px;
        }
        
        .docs-content p {
            margin-bottom: 15px;
            color: #34495e;
        }
        
        .docs-content ul, .docs-content ol {
            margin-bottom: 20px;
            padding-left: 30px;
        }
        
        .docs-content li {
            margin-bottom: 8px;
        }
        
        .docs-content code {
            background: #f8f9fa;
            padding: 2px 6px;
            border-radius: 4px;
            font-family: monospace;
        }
        
        .docs-content pre {
            background: var(--secondary);
            color: white;
            padding: 15px;
            border-radius: 8px;
            overflow-x: auto;
            margin-bottom: 20px;
        }
        
        .docs-content blockquote {
            border-left: 4px solid var(--primary);
            padding-left: 20px;
            margin-left: 0;
            color: #7f8c8d;
            font-style: italic;
        }
        
        .feature-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            margin: 20px 0;
        }
        
        .feature-card {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            border-left: 4px solid var(--primary);
        }
        
        .feature-card h4 {
            margin-bottom: 10px;
            color: var(--secondary);
        }
        
        .table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        
        .table th, .table td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        
        .table th {
            background: #f8f9fa;
            color: var(--secondary);
        }
        
        .table tr:hover {
            background: #f8f9fa;
        }
        
        /* Visualization Section */
        .visualization {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
            margin: 30px 0;
        }
        
        .chart-container {
            background: white;
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.08);
        }
        
        .architecture-diagram {
            width: 100%;
            margin: 20px 0;
            border-radius: 8px;
            overflow: hidden;
        }
        
        .flow-section {
            display: flex;
            justify-content: space-between;
            margin: 30px 0;
            position: relative;
        }
        
        .flow-step {
            text-align: center;
            width: 180px;
            position: relative;
            z-index: 2;
        }
        
        .flow-step .icon {
            width: 70px;
            height: 70px;
            background: var(--primary);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
            margin: 0 auto 15px;
        }
        
        .flow-connector {
            position: absolute;
            top: 35px;
            left: 0;
            right: 0;
            height: 3px;
            background: var(--primary);
            z-index: 1;
        }
        
        /* Backend Demo Section */
        .backend-demo {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
            margin: 30px 0;
        }
        
        .demo-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-top: 20px;
        }
        
        .demo-card {
            background: white;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.08);
        }
        
        .api-endpoint {
            background: var(--secondary);
            color: white;
            padding: 12px;
            border-radius: 6px;
            font-family: monospace;
            margin-bottom: 15px;
        }
        
        .response-box {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 6px;
            font-family: monospace;
            max-height: 200px;
            overflow-y: auto;
        }
        
        /* Toggle between MD and HTML view */
        .view-toggle {
            display: flex;
            justify-content: center;
            margin-bottom: 20px;
        }
        
        .toggle-btn {
            padding: 10px 20px;
            background: #eef2f7;
            border: none;
            cursor: pointer;
        }
        
        .toggle-btn.active {
            background: var(--primary);
            color: white;
        }
        
        /* Responsive Design */
        @media (max-width: 900px) {
            .container {
                flex-direction: column;
            }
            
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }
            
            .feature-grid, .demo-container {
                grid-template-columns: 1fr;
            }
            
            .flow-section {
                flex-direction: column;
                align-items: center;
                gap: 30px;
            }
            
            .flow-connector {
                width: 3px;
                height: 100%;
                top: 0;
                bottom: 0;
                left: 50%;
                transform: translateX(-50%);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Sidebar Navigation -->
        <div class="sidebar">
            <div class="logo">
                <h1><i class="fas fa-graduation-cap"></i> EduTrack</h1>
            </div>
            <ul class="nav-links">
                <li><a href="#" class="active"><i class="fas fa-home"></i> Introduction</a></li>
                <li><a href="#"><i class="fas fa-question-circle"></i> Problem Statement</a></li>
                <li><a href="#"><i class="fas fa-lightbulb"></i> Solution</a></li>
                <li><a href="#"><i class="fas fa-star"></i> Key Features</a></li>
                <li><a href="#"><i class="fas fa-layer-group"></i> Technology Stack</a></li>
                <li><a href="#"><i class="fas fa-sitemap"></i> Architecture</a></li>
                <li><a href="#"><i class="fas fa-database"></i> Database Design</a></li>
                <li><a href="#"><i class="fas fa-code"></i> API Endpoints</a></li>
                <li><a href="#"><i class="fas fa-download"></i> Installation</a></li>
                <li><a href="#"><i class="fas fa-road"></i> Future Enhancements</a></li>
            </ul>
        </div>
        
        <!-- Main Content -->
        <div class="main-content">
            <div class="header">
                <div class="search-box">
                    <i class="fas fa-search"></i>
                    <input type="text" placeholder="Search documentation..." id="searchInput">
                </div>
                <div class="user-actions">
                    <button class="btn btn-outline" id="exportBtn"><i class="fas fa-download"></i> Export PDF</button>
                    <button class="btn btn-primary" id="demoBtn"><i class="fas fa-code"></i> API Demo</button>
                </div>
            </div>
            
            <div class="view-toggle">
                <button class="toggle-btn active" id="renderedView">Rendered View</button>
                <button class="toggle-btn" id="markdownView">Markdown Source</button>
            </div>
            
            <div class="docs-container">
                <div class="docs-content" id="documentation-content">
                    <!-- Markdown content will be rendered here by JavaScript -->
                </div>
            </div>
        </div>
    </div>

    <script>
        // Markdown content for EduTrack documentation
        const markdownContent = `# EduTrack - Smart Curriculum & Attendance Management System

## Project Overview
EduTrack is a comprehensive digital solution designed to modernize and streamline academic administration for educational institutions. It replaces error-prone, paper-based systems with a centralized platform for managing student attendance and curriculum activities.

## Problem Statement

Educational institutions struggle with inefficient, manual processes for tracking attendance and managing curriculum activities. These traditional methods are:

- **Time-Consuming**: Teachers lose significant teaching time on manual roll calls
- **Prone to Errors**: Paper records and spreadsheets are easily misplaced or filled incorrectly
- **Lack Real-Time Insight**: Administrators and parents lack immediate access to attendance data
- **Ineffective for Engagement**: No streamlined way to schedule and track co-curricular activities

## Solution

EduTrack provides a unified web and mobile application that offers:

- **Flexible Attendance Marking**: Manual, QR code, and facial recognition options
- **Activity Management**: Tools to create, schedule, and track academic activities
- **Real-Time Reporting**: Instant access to attendance data and analytics
- **Automated Notifications**: Alerts to parents about attendance and activities
- **Centralized Dashboard**: Complete overview for administrators

## Key Features

### 1. Attendance Management
- Manual attendance marking with digital rosters
- QR code-based attendance system
- Facial recognition capability (future enhancement)
- Real-time attendance tracking

### 2. Curriculum Activity Management
- Schedule academic and co-curricular activities
- Set deadlines and reminders
- Track participation and completion

### 3. Reporting and Analytics
- Generate attendance reports (daily, weekly, monthly)
- Export data to PDF/Excel formats
- Visual analytics with charts and graphs

### 4. Notification System
- Email and SMS alerts for parents
- Activity reminders for students
- Administrative notifications

## Technology Stack

| Component | Technology |
|-----------|------------|
| Frontend | React.js, React Native, HTML5, CSS3 |
| Backend | Node.js, Express.js |
| Database | MongoDB with Mongoose |
| Authentication | JWT, bcrypt |
| Notifications | Twilio API, Nodemailer |
| Deployment | AWS/DigitalOcean |
| Facial Recognition | Face-api.js (Future) |

## System Architecture

EduTrack follows a modern three-tier architecture:

1. **Presentation Layer**: React.js web application and React Native mobile app
2. **Application Layer**: Node.js with Express.js RESTful API
3. **Data Layer**: MongoDB database with Mongoose ODM

![System Architecture](https://via.placeholder.com/800x400?text=EduTrack+System+Architecture+Diagram)

## Database Design

### Collections Overview

#### Users Collection
\`\`\`javascript
{
  _id: ObjectId,
  name: String,
  email: String,
  password: String,
  role: String, // admin, teacher, student, parent
  studentId: String, // if student
  children: [ObjectId], // if parent
  classes: [ObjectId] // if teacher
}
\`\`\`

#### Attendance Collection
\`\`\`javascript
{
  _id: ObjectId,
  studentId: ObjectId,
  classId: ObjectId,
  date: Date,
  status: String, // present, absent, late
  method: String, // manual, qr, facial
  timestamp: Date
}
\`\`\`

#### Activities Collection
\`\`\`javascript
{
  _id: ObjectId,
  title: String,
  description: String,
  type: String, // academic, co-curricular
  dueDate: Date,
  assignedTo: [ObjectId], // class or student IDs
  submissions: [{
    studentId: ObjectId,
    submissionDate: Date,
    status: String // completed, pending
  }]
}
\`\`\`

## API Endpoints

### Authentication Endpoints
- \`POST /api/auth/login\` - User login
- \`POST /api/auth/register\` - User registration
- \`POST /api/auth/forgot-password\` - Password reset request

### Attendance Endpoints
- \`POST /api/attendance/mark\` - Mark attendance
- \`GET /api/attendance/:classId\` - Get class attendance
- \`GET /api/attendance/student/:studentId\` - Get student attendance

### Activity Endpoints
- \`POST /api/activities\` - Create new activity
- \`GET /api/activities/class/:classId\` - Get class activities
- \`PUT /api/activities/:activityId\` - Update activity

### Report Endpoints
- \`GET /api/reports/attendance\` - Generate attendance reports
- \`GET /api/reports/activities\` - Generate activity reports

## Installation Guide

### Prerequisites
- Node.js (v14 or higher)
- MongoDB Atlas account or local MongoDB installation
- Git

### Setup Instructions

1. Clone the repository:
\`\`\`bash
git clone https://github.com/yourusername/edutrack.git
cd edutrack
\`\`\`

2. Install backend dependencies:
\`\`\`bash
cd backend
npm install
\`\`\`

3. Install frontend dependencies:
\`\`\`bash
cd ../frontend
npm install
\`\`\`

4. Set up environment variables:
Create a \`.env\` file in the backend directory with:
\`\`\`
MONGODB_URI=your_mongodb_connection_string
JWT_SECRET=your_jwt_secret
TWILIO_ACCOUNT_SID=your_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_token
EMAIL_USER=your_email
EMAIL_PASS=your_email_password
\`\`\`

5. Start the development servers:
\`\`\`bash
# Terminal 1 - Start backend
cd backend
npm run dev

# Terminal 2 - Start frontend
cd frontend
npm start
\`\`\`

6. Open your browser and navigate to \`http://localhost:3000\`

## Backend Integration Examples

### Example: Marking Attendance via API
\`\`\`javascript
// Example using fetch API
const markAttendance = async (studentId, classId, status) => {
  try {
    const response = await fetch('/api/attendance/mark', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': 'Bearer ' + localStorage.getItem('token')
      },
      body: JSON.stringify({ studentId, classId, status })
    });
    
    const data = await response.json();
    return data;
  } catch (error) {
    console.error('Error marking attendance:', error);
    throw error;
  }
};
\`\`\`

### Example: User Login
\`\`\`javascript
const login = async (email, password) => {
  try {
    const response = await fetch('/api/auth/login', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({ email, password })
    });
    
    const data = await response.json();
    
    if (data.success) {
      // Store token in localStorage
      localStorage.setItem('token', data.token);
      localStorage.setItem('user', JSON.stringify(data.user));
    }
    
    return data;
  } catch (error) {
    console.error('Login error:', error);
    throw error;
  }
};
\`\`\`

## Future Enhancements

1. **Facial Recognition Integration**
   - Implement facial recognition for contactless attendance
   - Use machine learning for improved accuracy

2. **Advanced Analytics**
   - Predictive analytics for at-risk students
   - Pattern recognition in attendance data

3. **Integration Capabilities**
   - LMS integration (Canvas, Moodle)
   - Student information system integration

4. **Mobile Features**
   - Geolocation-based attendance
   - Offline capability for remote areas

5. **Parent Portal**
   - Detailed progress tracking
   - Direct communication with teachers

## Development Team

- **Project Manager**: [Your Name]
- **Frontend Developer**: [Team Member]
- **Backend Developer**: [Team Member]
- **UI/UX Designer**: [Team Member]

## Support

For technical support or questions about EduTrack, please contact:

- **Email**: support@edutrack.com
- **Website**: https://www.edutrack.com
- **Documentation**: https://docs.edutrack.com

---

*Last Updated: October 26, 2023*
*Version: 1.0*
`;

        // Backend simulation for demo purposes
        const EduTrackBackend = {
            users: [
                { id: 1, email: "teacher@school.com", password: "password", role: "teacher", name: "Ravi Kumar" },
                { id: 2, email: "admin@school.com", password: "admin123", role: "admin", name: "School Admin" }
            ],
            attendance: [],
            classes: [
                { id: 1, name: "Class 10-A", teacher: "Ravi Kumar" },
                { id: 2, name: "Class 10-B", teacher: "Meena Sharma" }
            ],
            students: [
                { id: 1, name: "Rahul Sharma", class: "10-A" },
                { id: 2, name: "Priya Patel", class: "10-A" },
                { id: 3, name: "Amit Kumar", class: "10-A" }
            ],
            
            // Simulate API methods
            login(email, password) {
                return new Promise((resolve, reject) => {
                    setTimeout(() => {
                        const user = this.users.find(u => u.email === email && u.password === password);
                        if (user) {
                            resolve({ success: true, user, token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c" });
                        } else {
                            reject({ success: false, message: "Invalid credentials" });
                        }
                    }, 1000);
                });
            },
            
            markAttendance(studentId, status, method = "manual") {
                return new Promise((resolve) => {
                    setTimeout(() => {
                        const record = {
                            id: this.attendance.length + 1,
                            studentId,
                            status,
                            method,
                            date: new Date().toISOString()
                        };
                        this.attendance.push(record);
                        resolve({ success: true, record });
                    }, 500);
                });
            },
            
            getAttendanceStats() {
                const present = this.attendance.filter(a => a.status === "present").length;
                const total = this.attendance.length;
                const percentage = total > 0 ? Math.round((present / total) * 100) : 0;
                
                return {
                    present,
                    total,
                    percentage
                };
            },
            
            getStudents() {
                return new Promise((resolve) => {
                    setTimeout(() => {
                        resolve(this.students);
                    }, 500);
                });
            }
        };

        // DOM Elements
        const renderedViewBtn = document.getElementById('renderedView');
        const markdownViewBtn = document.getElementById('markdownView');
        const docsContent = document.getElementById('documentation-content');
        const exportBtn = document.getElementById('exportBtn');
        const demoBtn = document.getElementById('demoBtn');
        const searchInput = document.getElementById('searchInput');

        // Initialize page with rendered markdown
        docsContent.innerHTML = marked.parse(markdownContent);
        addVisualizations();

        // Add toggle functionality
        renderedViewBtn.addEventListener('click', () => {
            renderedViewBtn.classList.add('active');
            markdownViewBtn.classList.remove('active');
            docsContent.innerHTML = marked.parse(markdownContent);
            addVisualizations();
        });
        
        markdownViewBtn.addEventListener('click', () => {
            markdownViewBtn.classList.add('active');
            renderedViewBtn.classList.remove('active');
            docsContent.innerHTML = `<pre>${markdownContent}</pre>`;
        });

        // Export functionality
        exportBtn.addEventListener('click', () => {
            alert('PDF export functionality would be implemented here. This would generate a PDF version of the documentation.');
        });

        // Demo functionality
        demoBtn.addEventListener('click', () => {
            showBackendDemo();
        });

        // Search functionality
        searchInput.addEventListener('keyup', () => {
            const searchTerm = searchInput.value.toLowerCase();
            if (searchTerm.length > 2) {
                highlightSearchTerms(searchTerm);
            }
        });

        function addVisualizations() {
            // Add architecture diagram
            const architectureSection = document.querySelector('h2#system-architecture')?.parentElement || 
                                      document.querySelector('h2:contains("System Architecture")')?.parentElement;
            
            if (architectureSection) {
                const diagram = document.createElement('div');
                diagram.className = 'architecture-diagram';
                diagram.innerHTML = `
                    <div style="background:#f8f9fa; padding:20px; border-radius:8px;">
                        <h3 style="margin-top:0;">EduTrack System Architecture</h3>
                        <div style="display:flex; justify-content:space-between; margin:20px 0;">
                            <div style="text-align:center; width:30%;">
                                <div style="background:${getColor(0)}; color:white; padding:15px; border-radius:8px;">
                                    <h4>Presentation Layer</h4>
                                    <p>React.js Web App</p>
                                    <p>React Native Mobile</p>
                                </div>
                            </div>
                            <div style="text-align:center; width:30%;">
                                <div style="background:${getColor(1)}; color:white; padding:15px; border-radius:8px;">
                                    <h4>Application Layer</h4>
                                    <p>Node.js API</p>
                                    <p>Express.js Server</p>
                                </div>
                            </div>
                            <div style="text-align:center; width:30%;">
                                <div style="background:${getColor(2)}; color:white; padding:15px; border-radius:8px;">
                                    <h4>Data Layer</h4>
                                    <p>MongoDB Database</p>
                                    <p>Mongoose ODM</p>
                                </div>
                            </div>
                        </div>
                    </div>
                `;
                architectureSection.appendChild(diagram);
            }

            // Add API flow visualization
            const apiSection = document.querySelector('h2#api-endpoints')?.parentElement || 
                             document.querySelector('h2:contains("API Endpoints")')?.parentElement;
            
            if (apiSection) {
                const flowSection = document.createElement('div');
                flowSection.className = 'flow-section';
                flowSection.innerHTML = `
                    <div class="flow-step">
                        <div class="icon"><i class="fas fa-mobile-alt"></i></div>
                        <h4>Client Request</h4>
                        <p>Web or mobile app sends API request</p>
                    </div>
                    <div class="flow-step">
                        <div class="icon"><i class="fas fa-server"></i></div>
                        <h4>API Processing</h4>
                        <p>Node.js server processes the request</p>
                    </div>
                    <div class="flow-step">
                        <div class="icon"><i class="fas fa-database"></i></div>
                        <h4>Data Operations</h4>
                        <p>MongoDB performs CRUD operations</p>
                    </div>
                    <div class="flow-step">
                        <div class="icon"><i class="fas fa-reply"></i></div>
                        <h4>Response</h4>
                        <p>JSON response sent back to client</p>
                    </div>
                    <div class="flow-connector"></div>
                `;
                apiSection.appendChild(flowSection);
            }

            // Add database schema visualization
            const dbSection = document.querySelector('h2#database-design')?.parentElement || 
                            document.querySelector('h2:contains("Database Design")')?.parentElement;
            
            if (dbSection) {
                const schemaViz = document.createElement('div');
                schemaViz.className = 'visualization';
                schemaViz.innerHTML = `
                    <h3>Database Schema Visualization</h3>
                    <div style="display:flex; justify-content:space-between; flex-wrap:wrap; margin:20px 0;">
                        <div style="width:30%; background:white; padding:15px; border-radius:8px; margin-bottom:15px;">
                            <h4 style="color:${getColor(0)}; margin-top:0;">Users Collection</h4>
                            <ul style="font-size:0.9em;">
                                <li>_id: ObjectId</li>
                                <li>name: String</li>
                                <li>email: String</li>
                                <li>role: String</li>
                            </ul>
                        </div>
                        <div style="width:30%; background:white; padding:15px; border-radius:8px; margin-bottom:15px;">
                            <h4 style="color:${getColor(1)}; margin-top:0;">Attendance Collection</h4>
                            <ul style="font-size:0.9em;">
                                <li>_id: ObjectId</li>
                                <li>studentId: ObjectId</li>
                                <li>classId: ObjectId</li>
                                <li>status: String</li>
                            </ul>
                        </div>
                        <div style="width:30%; background:white; padding:15px; border-radius:8px; margin-bottom:15px;">
                            <h4 style="color:${getColor(2)}; margin-top:0;">Activities Collection</h4>
                            <ul style="font-size:0.9em;">
                                <li>_id: ObjectId</li>
                                <li>title: String</li>
                                <li>description: String</li>
                                <li>dueDate: Date</li>
                            </ul>
                        </div>
                    </div>
                `;
                dbSection.appendChild(schemaViz);
            }
        }

        function showBackendDemo() {
            const demoHTML = `
                <div class="backend-demo">
                    <h2>Backend API Demo</h2>
                    <p>Try out these simulated API endpoints to understand how EduTrack's backend works:</p>
                    
                    <div class="demo-container">
                        <div class="demo-card">
                            <h3>Login API</h3>
                            <div class="api-endpoint">POST /api/auth/login</div>
                            <div class="form-group">
                                <label>Email:</label>
                                <input type="email" id="demoEmail" value="teacher@school.com" class="form-control">
                            </div>
                            <div class="form-group">
                                <label>Password:</label>
                                <input type="password" id="demoPassword" value="password" class="form-control">
                            </div>
                            <button class="btn btn-primary" onclick="demoLogin()">Test Login</button>
                            <div class="response-box" id="loginResponse"></div>
                        </div>
                        
                        <div class="demo-card">
                            <h3>Mark Attendance</h3>
                            <div class="api-endpoint">POST /api/attendance/mark</div>
                            <div class="form-group">
                                <label>Student ID:</label>
                                <select id="demoStudent" class="form-control">
                                    <option value="1">Rahul Sharma (10-A)</option>
                                    <option value="2">Priya Patel (10-A)</option>
                                    <option value="3">Amit Kumar (10-A)</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <label>Status:</label>
                                <select id="demoStatus" class="form-control">
                                    <option value="present">Present</option>
                                    <option value="absent">Absent</option>
                                    <option value="late">Late</option>
                                </select>
                            </div>
                            <button class="btn btn-primary" onclick="demoMarkAttendance()">Mark Attendance</button>
                            <div class="response-box" id="attendanceResponse"></div>
                        </div>
                    </div>
                    
                    <div style="margin-top:20px;">
                        <button class="btn btn-outline" onclick="closeDemo()">Close Demo</button>
                    </div>
                </div>
            `;
            
            docsContent.innerHTML = demoHTML + docsContent.innerHTML;
        }

        function demoLogin() {
            const email = document.getElementById('demoEmail').value;
            const password = document.getElementById('demoPassword').value;
            const responseBox = document.getElementById('loginResponse');
            
            responseBox.innerHTML = "Calling API...";
            
            EduTrackBackend.login(email, password)
                .then(response => {
                    responseBox.innerHTML = JSON.stringify(response, null, 2);
                })
                .catch(error => {
                    responseBox.innerHTML = JSON.stringify(error, null, 2);
                });
        }

        function demoMarkAttendance() {
            const studentId = document.getElementById('demoStudent').value;
            const status = document.getElementById('demoStatus').value;
            const responseBox = document.getElementById('attendanceResponse');
            
            responseBox.innerHTML = "Calling API...";
            
            EduTrackBackend.markAttendance(studentId, status)
                .then(response => {
                    responseBox.innerHTML = JSON.stringify(response, null, 2);
                })
                .catch(error => {
                    responseBox.innerHTML = JSON.stringify(error, null, 2);
                });
        }

        function closeDemo() {
            docsContent.innerHTML = marked.parse(markdownContent);
            addVisualizations();
        }

        function highlightSearchTerms(term) {
            const content = docsContent.innerHTML;
            const regex = new RegExp(term, 'gi');
            const highlighted = content.replace(regex, match => `<span style="background-color:yellow;">${match}</span>`);
            docsContent.innerHTML = highlighted;
        }

        function getColor(index) {
            const colors = ['#3498db', '#2ecc71', '#e74c3c', '#f39c12', '#9b59b6', '#1abc9c'];
            return colors[index % colors.length];
        }

        // Make functions available globally for onclick attributes
        window.demoLogin = demoLogin;
        window.demoMarkAttendance = demoMarkAttendance;
        window.closeDemo = closeDemo;

        // Initialize the page
        console.log("EduTrack Documentation Loaded");
        console.log("Backend simulator initialized with API methods:");
        console.log("- login(email, password)");
        console.log("- markAttendance(studentId, status)");
        console.log("- getAttendanceStats()");
        console.log("- getStudents()");
    </script>
</body>
</html>
